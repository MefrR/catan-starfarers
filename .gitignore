node_modules/
dist/
*.tsbuildinfo
.DS_Store
# env files (Supabase keys, server secrets) — never commit any of these
.env
.env.*
!.env.example
# downloaded Google OAuth client-secret JSON — must never be committed
client_secret_*.json
# extracted rulebook text (regenerated from PDFs)
rulebook.txt
almanac.txt
# official Catan rulebooks — copyrighted, reference only, never publish
*.pdf
# official Catan art used only as visual reference — never publish; we ship original vectors
*.png
# ...except our own shipped public assets (logo, icons) which MUST be deployed
!client/public/*.png

# local audit backup (not source of truth)
backup/
